{
  "name": "ai-career-path-recommendation-system",
  "private": true,
  "description": "Root scripts — React app lives in \"Front End\"",
  "scripts": {
    "dev": "npm run dev --prefix \"Front End\"",
    "build": "npm run build --prefix \"Front End\"",
    "install:front": "npm install --prefix \"Front End\"",
    "lint": "npm run lint --prefix \"Front End\""
  }
}
